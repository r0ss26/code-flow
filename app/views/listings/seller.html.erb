<% if !@active_listings.empty? %>
  <%= render partial: "listing", collection: @active_listings %>
<% end %>

<% if !@inactive_listings.empty? %>
  <%= render partial: "listing", collection: @inactive_listings %>
<% end %>

<% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path %>
<% end %>
