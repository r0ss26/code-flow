<%= form_with(model: [@listing, @order, review], local: true) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.text_field :rating %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :review_poster_id %>
    <%= form.text_field :review_poster_id %>
  </div>

  <div class="field">
    <%= form.label :review_receiver_id %>
    <%= form.text_field :review_receiver_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
