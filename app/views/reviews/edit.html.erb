<h1>Editing Review</h1>

<%= render 'form', review: @review %>

<%= link_to 'Show', listing_order_review_path(@listing, @order, @review) %> |
<%= link_to 'Back', listing_order_reviews_path(@listing, @order, @review) %>
